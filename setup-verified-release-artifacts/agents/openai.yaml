interface:
  display_name: "Setup Verified Release Artifacts"
  short_description: "Verify releases without an installer"
  default_prompt: "Use $setup-verified-release-artifacts to add SHA256SUMS, artifact attestations where honest, immutable release verification, and concise README verification instructions to this release-only repo with no installer path."
