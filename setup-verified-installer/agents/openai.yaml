interface:
  display_name: "Setup Verified Installer"
  short_description: "Create verified installers and signed releases"
  default_prompt: "Use $setup-verified-installer to add or harden an installer, including release-asset checksums, attestations, and prompt-free signed release refs when this repo also publishes downloadable artifacts."
